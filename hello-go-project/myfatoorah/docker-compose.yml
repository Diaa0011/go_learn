services:
  db:
    image: postgres:16-alpine
    container_name: myfatoorah_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Test2222!!!!
      POSTGRES_DB: myfatoorah_invoices

    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: